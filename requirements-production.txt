# Production-specific dependencies (optional)

# Redis caching (for distributed deployments)
redis>=5.0.0

# API frameworks (choose one)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
# OR
flask>=3.0.0
gunicorn>=21.2.0

# Monitoring and metrics
prometheus-client>=0.19.0

# Email alerts (optional)
aiosmtplib>=3.0.0
